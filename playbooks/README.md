# Playbooks

Playbooks are reusable instructions for ebiii. Add each playbook as a Markdown
file in this directory with `name` and `description` in its frontmatter.

Keep this file as a short index of the available playbooks.
